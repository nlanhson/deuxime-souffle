// Web stub for @react-native-segmented-control/segmented-control (Flow-typed,
// native-only). Segmented chooses the JS pill variant on web, so this is never
// rendered — it exists only to satisfy esbuild's static import graph.
import { createElement } from 'react';
const SegmentedControl = () => createElement('div', null);
export default SegmentedControl;
