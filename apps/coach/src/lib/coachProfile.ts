/**
 * The signed-in coach's real profile photo. A real human portrait (not an initials glyph or
 * silhouette) — shared by the Profile screen and the header avatars so the same face shows
 * everywhere. Real code replaces this with the URL returned from the avatar upload (PRD §5).
 */
export const COACH_PHOTO = 'https://randomuser.me/api/portraits/men/32.jpg';

/** The signed-in coach's display name — shared by the Profil tab identity card and Settings. */
export const COACH_NAME = 'Karim Benali';
